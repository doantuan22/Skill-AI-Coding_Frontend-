# Developer product fixture

Active Structure Lock v1 for the marketing home of a CLI + SDK for database migrations.

Locked blocks: `B1` value proposition + install command + docs link, `B2` how it works (3 steps: define, diff, apply), `B3` supported databases, `B4` code example, `B5` comparison with manual migration scripts, `B6` open-source/community proof (GitHub stars count supplied by user), `B7` docs CTA.

Content: real CLI output transcripts, TypeScript/Go code samples, a supported-database list. Stack: Next.js + Tailwind; existing `next/font` usage with Inter. No animation library.
