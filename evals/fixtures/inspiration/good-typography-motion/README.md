# Good typography and motion fixture

Established editorial/SaaS hybrid product with a documented type system (single variable sans with optical sizes + mono for code; 7-role scale; 65ch measure; tabular numerals in tables; Vietnamese verified), motion tokens (`fast 120ms`, `base 200ms`, `slow 320ms`, three named easings), one restrained hero line reveal, IO-based section reveals, complete `prefers-reduced-motion` equivalents, and no scroll handlers.

User request: "Audit our typography and animation and improve them."
