# Existing Playwright fixture

Project metadata declares `@playwright/test`, `playwright.config.ts`, package manager lockfile, and existing e2e fixtures. Detection should reuse it without installation or overwrite; browser readiness remains runtime-dependent.
