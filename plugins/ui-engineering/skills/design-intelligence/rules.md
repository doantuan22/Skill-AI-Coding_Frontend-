# Design intelligence rules

Treat product, audience, task, density, and platform as inputs to visual decisions. Use the smallest relevant reference set. A reference is a constraint or option, not a command to imitate another product.

If the existing system is serviceable, choose `keep` or `refine` over an unnecessary replacement. If the direction implies a structural change, stop and request Phase 1 rollback.
