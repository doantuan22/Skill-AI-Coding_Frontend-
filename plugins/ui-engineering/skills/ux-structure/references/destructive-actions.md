# Destructive actions

Use when an action deletes, revokes, permanently changes, or creates a costly business consequence. Specify affected object, permission, consequence, confirmation requirement, irreversible point, cancellation, success, failure, and recovery path.

Never make such an action indistinguishable from routine actions or decide that confirmation/undo is unnecessary without confirmed product rules.
