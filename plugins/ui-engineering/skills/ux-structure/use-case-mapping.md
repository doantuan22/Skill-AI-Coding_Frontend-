# Use-case mapping

Create `USE-CASE-MAP.md` by connecting actor → goal → use case → page or interaction. Each use case needs an ID, actor, goal, trigger, required information, primary action, system response, next state, and plausible failure condition.

Required use cases must trace to at least one flow and page. A page may support several related use cases, but unrelated goals signal a page-responsibility problem.
