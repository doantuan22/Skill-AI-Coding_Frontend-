"""Runtime engine: read-only capability detection, optional Playwright browser runner, evidence and probes.

Playwright is an optional, project-local runtime dependency. Nothing here imports or installs it; the browser
helper is a Node script executed only when a runtime capture is explicitly requested.
"""
