# Table realization

Implement locked columns/actions with consistent headers, row behavior, selection, empty/loading/error states, pagination, and responsive transformation. On narrow viewports, choose the declared semantic alternative rather than forcing an unusable wide table.
