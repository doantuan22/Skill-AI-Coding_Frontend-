# Spacing

Use a coherent scale derived from the Design System. Apply it to layout gaps, component padding, section rhythm, and density. Treat one-off values as candidates for a documented exception or tokenization; do not normalize values without understanding the existing system.
