# Accessibility issue model

```yaml
id:
source: axe
page_id:
route:
viewport:
rule:
severity:
category:
description:
evidence:
status: CONFIRMED
iteration:
```

Fingerprint is source + rule ID + page + viewport + target. False positives and accepted exceptions need a reason, scope, and approval source; AI may not self-exempt critical issues.
